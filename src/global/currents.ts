import { ref } from 'vue'

export const currentList = ref()
export const currentListOption = ref({ id: null } as any)
export const currentConfirmation = ref(false)
export const currentDropColor = ref()